# Fluent Python

按《Fluent Python》阅读顺序整理示例。每个章节目录可以包含：

- `examples/`：可运行代码
- `notes.md`：概念、陷阱和实验结论
- `tests/`：对行为的验证

当前示例：`01_data_model/vector.py`，演示 `__repr__`、`__abs__`、`__bool__`、`__len__` 和 `__add__` 等数据模型方法。

